spring:
  data:
    cassandra:
      contact-points: localhost
      port: 9042
      keyspace-name: clearflow_dev
      local-datacenter: datacenter1
      schema-action: create_if_not_exists
  kafka:
    bootstrap-servers: localhost:29092
